[package]
name = "pathfinding"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Pathfinding algorithms for cave navigation — provides A*, BFS, and other graph search implementations used by the robot for both forward exploration and return navigation via stored map."

[dependencies]
