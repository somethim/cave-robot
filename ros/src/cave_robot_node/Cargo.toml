[workspace]

[package]
name = "cave_robot_node"
version = "0.1.0"
edition = "2021"

[dependencies]
rclrs = "0.7"
sensor_msgs = "5.3.7"
geometry_msgs = "5.3.7"
serde_json = "1"
shared = { path = "../../../crates/shared" }
# shared depends on serde, dotenvy, rand — all compatible
