This directory holds Gazebo world assets for the `cave_robot_gazebo` package.

Planned layout:

- `generated/` for cave worlds derived from `cave.json`
- hand-authored support worlds if needed for testing

The Phase 1 install rules package this directory to:

- `share/cave_robot_gazebo/worlds/`
